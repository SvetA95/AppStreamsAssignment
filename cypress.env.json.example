{
  "_comment": "Local overrides for the defaults in cypress.config.ts — git-ignored. Copy to cypress.env.json and edit if you need a different account or environment; the values below are the assignment's own working defaults, already baked into config.",
  "EMAIL": "test_qa_ex@example.com",
  "PASSWORD": "test_qa_ex@example.com",
  "API_URL": "https://dev.api.avtoikonom.com"
}
